const port = Number(process.env.PORT ?? 3000);

console.log("{{PROJECT_DISPLAY_NAME}} starter created successfully.");
console.log(`Configured port: ${port}`);
console.log("Fastify server implementation will be added by the next LaunchStack template layer.");
